const navToggle=document.querySelector('.nav-toggle');const navMenu=document.querySelector('#nav-menu');navToggle?.addEventListener('click',()=>{const open=navMenu.classList.toggle('open');navToggle.setAttribute('aria-expanded',open)});navMenu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>navMenu.classList.remove('open')));
const slides=[...document.querySelectorAll('.slide')];let current=0;function showSlide(i){slides[current].classList.remove('active');current=(i+slides.length)%slides.length;slides[current].classList.add('active')}document.querySelector('[data-slide="next"]')?.addEventListener('click',()=>showSlide(current+1));document.querySelector('[data-slide="prev"]')?.addEventListener('click',()=>showSlide(current-1));setInterval(()=>showSlide(current+1),6500);
const search=document.querySelector('#service-search');const services=[...document.querySelectorAll('.service')];const empty=document.querySelector('.empty-state');search?.addEventListener('input',()=>{const q=search.value.toLowerCase().trim();let visible=0;services.forEach(card=>{const hay=(card.textContent+' '+card.dataset.keywords).toLowerCase();const match=!q||hay.includes(q);card.style.display=match?'block':'none';if(match)visible++});if(empty)empty.hidden=visible!==0});